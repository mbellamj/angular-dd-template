export const FILE_SIZE_LIMIT = 50 * Math.pow(10, 6);

export const ALLOWED_FILE_TYPE: string = 'image/png,image/gif,image/jpeg,image/bmp,image/webp';

export const ALLOWED_EXTENSIONS = /\.(jpe?g|png|gif|webp)$/i;
